.DS_Store
*.log
# Syncthing conflict copies — the folder is synced; debris must not reach git
*.sync-conflict-*
.env
.env.*

# BMad tooling install and planning/implementation artifacts — local working
# state, not source for this repo
_bmad/
_bmad-output/